a) Future object will hold the result for executor.submit(f::m)
b) Every piece of code can be forced to access by one thread by using synchronised keyword, or 
putting the method, under synchronised block